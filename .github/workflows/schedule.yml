name: Hello, World

on:
  schedule:
    - cron: '*/2 * * * 1-5'

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: "Echo Current Time"
        run: echo "The current server time is $(date)"